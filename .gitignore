# ── 개인 데이터 (회사 URL/이메일/내부 IP 포함 — 절대 공개 금지) ──
config.json
config.json.bak
favorites.json

# ── 빌드 산출물 (사용자명 등 로컬 경로 포함) ──
build/
dist/
installer/
__pycache__/
*.pyc

# ── 백업 (옛 config 사본 포함) ──
backups/

# ── 다운로드 페이지 생성물 ──
download_page/_site/
download_page/_release/
